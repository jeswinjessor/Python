def mean(mylist):
    the_mean = sum(mylist) / len(mylist)
    return the_mean

print(mean([1,2,3,4]))

# if we don't have a return statement the function return 'None'
# A return statement is recommended in a python function
# using print inside a function instead of return is not recommended 


